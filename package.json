{
  "name": "statistica-mikexcohen-libro",
  "version": "1.0.0",
  "description": "Libro interattivo di statistica (corso Mike X Cohen) — build self-contained HTML",
  "private": true,
  "scripts": {
    "build": "python3 build_book.py book.json _tmp.html && node render_math.js _tmp.html Statistica.html && rm -f _tmp.html"
  },
  "dependencies": {
    "mathjax-full": "^3.2.2"
  }
}